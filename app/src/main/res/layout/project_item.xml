<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/proj_margin"
        android:layout_marginTop="@dimen/double_small_margin"
        android:layout_marginRight="@dimen/proj_margin"
        android:layout_marginBottom="@dimen/double_small_margin"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="@dimen/card_radius"
        app:cardElevation="@dimen/proj_margin"
        app:cardMaxElevation="@dimen/card_elevation"
        app:cardPreventCornerOverlap="true">

        <TextView
            android:id="@+id/projectNameTv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:layout_margin="@dimen/proj_tv_margin"
            android:text="@string/project_name"
            android:textSize="@dimen/big_text_size"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/shareBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginEnd="@dimen/proj_margin_end"
            app:srcCompat="@android:drawable/ic_menu_share" />

        <ImageButton
            android:id="@+id/deleteBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginEnd="@dimen/double_small_margin"
            app:srcCompat="@android:drawable/ic_menu_delete" />
    </androidx.cardview.widget.CardView>


    <ListView
        android:id="@+id/projectSearchResList"
        android:layout_width="match_parent"
        android:layout_height="@dimen/proj_list_size"
        android:visibility="gone"/>

</LinearLayout>
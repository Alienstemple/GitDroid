<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".presentation.AuthActivity">

    <EditText
        android:id="@+id/enterEmailEditText"
        android:layout_width="0dp"
        android:layout_height="42dp"
        android:layout_marginTop="240dp"
        android:hint="Enter email"
        app:layout_constraintEnd_toEndOf="@+id/authBtn"
        app:layout_constraintStart_toStartOf="@+id/authBtn"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/authBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="#fff"
        android:drawableLeft="@drawable/icon_github_small"
        android:drawablePadding="8dp"
        android:padding="8dp"
        android:text="Authorize on GitHub"
        android:textColor="#000"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/enterEmailEditText" />

</androidx.constraintlayout.widget.ConstraintLayout>